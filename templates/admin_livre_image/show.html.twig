{% extends 'base-backend.html.twig' %}

{% block title %}LivreImage{% endblock %}

{% block body %}
        <a href="{{ path('app_admin_livre_image_index') }}" class="btn btn-dark">Retour à la liste</a>
    <h1>LivreImage</h1>
        <div class="d-flex justify-content-end align-items-end">
        {{ include('admin_livre_image/_delete_form.html.twig') }}
        <a href="{{ path('app_admin_livre_image_edit', {'id': livre_image.id}) }}"class="btn btn-success ms-2"><i class="align-middle" data-feather="edit-2"></i></a>
        </div>   

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livre_image.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ livre_image.nom }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ livre_image.imageName }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ livre_image.updatedAt ? livre_image.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    

    

{% endblock %}
