{% extends 'base-backend.html.twig' %}

{% block title %}Auteur{% endblock %}

{% block body %}
<a href="{{ path('app_admin_auteur_index') }}" class="btn btn-dark">Retour à la liste</a>
    <h1>Auteur</h1>
        <div class="d-flex justify-content-end align-items-end">
        {{ include('admin_auteur/_delete_form.html.twig') }}
        <a href="{{ path('app_admin_auteur_edit', {'id': auteur.id}) }}"class="btn btn-success ms-2"><i class="align-middle" data-feather="edit-2"></i></a>
        </div> 
        <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ auteur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ auteur.prenom }}</td>
            </tr>
            <tr>
                <th>Peseudo</th>
                <td>{{ auteur.peseudo }}</td>
            </tr>
            <tr>
                <th>Biographie</th>
                <td>{{ auteur.biographie }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ auteur.imageName }}</td>
            </tr>
            <tr>
                <th>UpddatedAt</th>
                <td>{{ auteur.upddatedAt ? auteur.upddatedAt|date('d-m-Y H:i:s') : 'N/A' }}</td>
            </tr>
        </tbody>
    </table>

 

    

{% endblock %}
